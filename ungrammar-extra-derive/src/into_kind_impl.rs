// Standard Uses

// Crate Uses
use crate::derive_enum::DeriveEnum;

// External Uses
//use virtue::prelude::*;
use eyre::Result;

#[allow(unused)]
impl DeriveEnum {
    pub(crate) fn generate_into_impl() -> Result<()> {
        todo!()
    }
}
